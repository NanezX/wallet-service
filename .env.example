# Copia este archivo a .env antes de correr docker compose
# cp .env.example .env && docker compose up --build

# Secreto para firmar JWT
JWT_SECRET=change-me

# Solo para desarrollo local SIN Docker
# DATABASE_URL=postgres://wallet:wallet@localhost:5432/wallet_service
